$(function () {	// if logged, change right-top corner to username	if (typeof $.cookie('logged') === 'undefined'){			} else {		$("#changeIfLogged").html("<a href='customer-account.php'>Welcome Back " + $.cookie('logged') + "</a>"+"&nbsp;&nbsp;&nbsp;<a id='logout'>Log out</a>");	}		// if click logout, clean the cookie	$("#logout").click(function(){		$.removeCookie('logged', { path: '/' });		window.location.href = "index.php";	});		// log in is forced befor checkout 	$("#beforeCheckout").click(function(){		if (typeof $.cookie('logged') === 'undefined'){			alert ("Before purchase, please register/login first!");			window.location.href = "register.php";		}		else{			window.location.href = "checkout1.php";		}	});		// if click logout, clean the cookie	$("#accPageLogout").click(function(){		$.removeCookie('logged', { path: '/' });		window.location.href = "index.php";	});})function test(){	alert($.cookie('logged'));}